# Design Mood Board - Progress Log

## How to Use

After each completed loop step, append one entry with step id, label, skills applied, outcome, and ISO timestamp.

### Entry format
<!-- STEP step_id="{N.M}" label="{short label}" skills="{skill-a, skill-b}" outcome="PASS|FAIL|PARTIAL" at="{ISO8601}" -->

---

(empty - Ralph fills entries during execution)
